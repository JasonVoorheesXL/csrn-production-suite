name: Validate CSRN Suite

on:
  push:
    branches:
      - develop-1.13
      - release/1.13-foundation
      - "phase/**"
  pull_request:
    branches:
      - develop-1.13
      - release/1.13-foundation
      - "phase/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pytest

      - name: Apply optional phase integration
        run: python tools/ci_prepare.py

      - name: Focused diagnostic
        shell: bash
        run: |
          python -m pytest -q tests/test_caption_service.py tests/test_caption_routes.py tests/test_caption_worker.py tests/test_caption_architecture.py tests/test_phase_6_4_migration.py > phase64-${{ runner.os }}.log 2>&1 || true
          cat phase64-${{ runner.os }}.log

      - name: Upload focused diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: phase-6-4-${{ runner.os }}
          path: phase64-${{ runner.os }}.log

      - name: Validate repository
        run: python tools/dev_workflow.py validate
