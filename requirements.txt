# CSRN runtime lock for CPython 3.13.14.
# Keep every production dependency exact so setup and CI resolve the same graph.
Flask==3.0.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.2
blinker==1.9.0
colorama==0.4.6
waitress==3.0.0
websocket-client==1.8.0
Pillow==12.3.0
anyio==4.14.2
certifi==2026.7.22
filelock==3.32.2
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
numpy==2.5.2
packaging==26.2
PyYAML==6.0.3
setuptools==84.0.0
typing-extensions==4.16.0
# Roster name-pronunciation (roster_service.py imports `pronouncing`, which
# pulls `cmudict`). These were installed in the working venv but never locked,
# so a missing install used to surface only at first roster-service use --
# never in tools/environment_check.py, which only verifies what this file
# lists. The two importlib-* backports + zipp are cmudict's declared deps
# (Requires-Dist); pinned here so `pip install -r` resolves one exact graph.
pronouncing==0.3.0
cmudict==1.1.3
importlib-metadata==9.0.0
importlib-resources==7.1.0
zipp==4.1.0
sounddevice==0.5.5
av==18.0.0
ctranslate2==4.8.1
faster-whisper==1.2.1
flatbuffers==25.12.19
huggingface-hub==1.27.0
onnxruntime==1.28.0
protobuf==7.35.1
tokenizers==0.23.1
tqdm==4.70.0
playwright==1.62.0
